<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.labawsrh.aws.introscreen">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdminLogin" />
        <activity android:name=".AdminRegister" />
        <activity android:name=".firstpage" />
        <activity android:name=".category" />
        <activity android:name=".volunteer" />
        <activity android:name=".home" />
        <activity android:name=".video_sign_in" />
        <activity android:name=".educationMainActivity"/>
        <activity android:name=".educationViewImageActivity"/>
        <activity android:name=".educationAfterRecy"/>
        <activity android:name=".animalMainActivity"/>
        <activity android:name=".animalAfterRecy"/>
        <activity android:name=".animalViewImageActivity"/>
        <activity android:name=".womanMainActivity"/>
        <activity android:name=".womanAfterRecy"/>
        <activity android:name=".womanViewImageActivity"/>
        <activity android:name=".enivorMainActivity"/>
        <activity android:name=".enivorAfterRecy"/>
        <activity android:name=".enivorViewImageActivity"/>
        <activity android:name=".healthViewImageActivity"/>
        <activity android:name=".healthMainActivity"/>
        <activity android:name=".healthAfterRecy"/>
        <activity android:name=".eventViewImageActivity"/>
        <activity android:name=".eventAfterRecy"/>
        <activity android:name=".eventMainActivity"/>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".MainActivity" />
        <activity android:name=".MobileNoActitvity" />
        <activity android:name=".verifyPhoneActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".GridLayout" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>